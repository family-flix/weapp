<block wx:if="{{mounted}}">
    <cover-view class="presence class-name {{open ? openClassName : closedClassName}}" role="presentation" bindanimationend="handleAnimationEnd">
        <slot />
    </cover-view>
</block>
