<cover-view wx:if="{{cover}}" class="icon icon-{{name}} {{className}}" style="display: block; font-size: {{size}}px; width: {{size}}px; height: {{size}}px; {{style}}"></cover-view>
<text wx:else class="icon icon-{{name}} {{className}}" style="display: block; font-size: {{size}}px; width: {{size}}px; height: {{size}}px; {{style}}"></text>
