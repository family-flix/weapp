<block wx:if="{{mounted}}">
    <view class="presence class-name {{open ? openClassName : closedClassName}}" role="presentation" data-state="{{open ? 'open' : 'closed'}}" bind:animationend="handleAnimationEnd">
        <slot />
    </view>
</block>
