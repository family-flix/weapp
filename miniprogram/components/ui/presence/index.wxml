<block wx:if="{{mounted}}">
    <view class="presence class" role="presentation" data-state="{{open ? 'open' : 'closed'}}" bind:animationend="handleAnimationEnd">
        <slot />
    </view>
</block>
